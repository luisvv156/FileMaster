# ── GUI ───────────────────────────────────────────────────────────────────────
pywebview>=4.0                  # Ventana de escritorio con HTML/CSS/JS
customtkinter>=5.2,<6           # (legacy, ya no se usa con pywebview)

# ── IA y embeddings ───────────────────────────────────────────────────────────
sentence-transformers>=2.7.0    # Embeddings semánticos SBERT (all-MiniLM-L6-v2)
scikit-learn>=1.3               # Usado internamente por sentence-transformers
torch>=2.0                      # Backend de sentence-transformers
spacy>=3.7                      # Extracción de keywords con POS tagging

# ── Extracción de texto ───────────────────────────────────────────────────────
pdfplumber>=0.10                # Extracción de texto en PDFs
python-docx>=1.1                # Lectura de archivos .docx
python-pptx>=0.6                # Lectura de archivos .pptx
openpyxl>=3.1                   # Lectura de archivos .xlsx
pytesseract>=0.3                # OCR para PDFs escaneados (requiere Tesseract instalado)
Pillow>=10.0                    # Procesamiento de imágenes para OCR

# ── Utilidades ────────────────────────────────────────────────────────────────
numpy>=1.24                     # Operaciones vectoriales
python-dateutil>=2.8            # Parsing de fechas para renombrado

# ── Empaquetado ───────────────────────────────────────────────────────────────
pyinstaller>=6,<7               # Compilar a .exe
